{
  "name": "ascii-studio",
  "version": "1.0.0",
  "private": true,
  "description": "ASCII Studio - Turn Pixels Into Characters. Full-stack image-to-ASCII-art platform.",
  "scripts": {
    "dev": "concurrently -n server,client -c green,cyan \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run build --prefix client",
    "test": "npm test --prefix server",
    "lint": "npm run lint --prefix server && npm run lint --prefix client",
    "docker:up": "docker compose up --build",
    "docker:down": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
